import React from 'react';
import PageBase from 'Pages/common/PageBase';

const JsonPageView = () => (
  <PageBase title="Json Watchers" controls={<div>controls</div>} body={<div>body</div>} />
);

export default JsonPageView;
