import React from 'react';
import PageBase from 'Pages/common/PageBase';
import Controls from './Controls';

const JsonPageView = () => (
  <PageBase title="Json Watchers" controls={<Controls />} body={<div>body</div>} />
);

export default JsonPageView;
